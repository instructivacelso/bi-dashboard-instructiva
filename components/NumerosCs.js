import { numero, pct, moeda } from '@/lib/formato.js';
import { FAIXAS } from '@/lib/cscx.js';
const L = ({ itens }) => <div className="leitura">{itens.map(([r, v]) => <div key={r}><span>{r}</span><b>{v}</b></div>)}</div>;
export const Base = ({ n }) => <L itens={[['Alunos ativos', numero(n.ativos)], ['Novos no período', numero(n.novos)], ['Suspensos', numero(n.suspensos)], ['Cancelados', numero(n.cancelados)], ['Concluídos', numero(n.concluidos)], ...n.porCurso.map((c) => [c.chave, numero(c.n)])]} />;
export const Onboarding = ({ n }) => { const o = n.onboarding; return <L itens={[['Compras', numero(o.compras)], ['Boas-vindas', numero(o.boas_vindas)], ['Acesso liberado', numero(o.liberados)], ['Orientados', numero(o.orientados)], ['No grupo', numero(o.no_grupo)], ['1º acesso', numero(o.acessaram)], ['Iniciaram o curso', numero(o.iniciaram)], ['Compra → acesso', o.dias_acesso === null ? 'sem dados' : `${numero(o.dias_acesso, 1)} dias`], ['Taxa de ativação', pct(o.taxaAtivacao)]]} />; };
export const Engajamento = ({ n }) => { const e = n.engajamento; return <L itens={[['Ativos (7 dias)', numero(e.engajados)], ['Inativos (+15 dias)', numero(e.inativos)], ['Nunca acessaram', numero(e.nunca_acessaram)], ['Progresso médio', e.progresso_medio === null ? 'sem dados' : `${numero(e.progresso_medio)}%`], ['Com 50%+', numero(e.progredindo)], ['Concluíram', numero(e.concluiram)], ['Engajamento', pct(e.taxaEngajamento)], ['Inatividade', pct(e.taxaInatividade)]]} />; };
export const Health = ({ n }) => { const h = n.health; return <L itens={[...Object.keys(FAIXAS).map((k) => [FAIXAS[k], numero(h[k])]), ['Média', h.media === null ? 'sem dados' : numero(h.media)], ['Valor em risco', moeda(h.valorEmRisco, 0)], ...h.cursosRisco.map((c) => [`Risco · ${c.chave}`, numero(c.n)])]} />; };
export const Satisfacao = ({ n }) => { const s = n.satisfacao; return <L itens={[['Respostas', numero(s.respostas)], ['NPS', s.nps === null ? 'sem dados' : numero(s.nps)], ['Promotores', numero(s.promotores)], ['Neutros', numero(s.neutros)], ['Detratores', numero(s.detratores)], ['CSAT', pct(s.csat)], ['CES (1 a 5)', s.ces === null ? 'sem dados' : numero(s.ces, 1)]]} />; };
export const Voz = ({ n }) => { const v = n.voz; return <L itens={[['Reclamações', numero(v.reclamacoes)], ['Elogios', numero(v.elogios)], ['Sugestões', numero(v.sugestoes)], ['Graves', numero(v.graves)], ['Abertas', numero(v.abertas)], ['Voz do Cliente', numero(v.voz_cliente)], ['Taxa de reclamação', pct(v.taxaReclamacao)], ...v.categorias.map((c) => [`Etapa · ${c.chave}`, numero(c.n)])]} />; };
export const Retencao = ({ n }) => { const r = n.retencao; return <L itens={[['Pedidos de cancelamento', numero(r.pedidos_cancelamento)], ['Pedidos de reembolso', numero(r.pedidos_reembolso)], ['Em aberto', numero(r.abertos)], ['Recuperados', numero(r.recuperados)], ['Cancelamentos concluídos', numero(r.concluidos)], ['Taxa de recuperação', pct(r.taxaRecuperacao)], ['Receita preservada', moeda(r.preservada, 0)], ['Receita perdida', moeda(r.perdida, 0)]]} />; };
export const Expansao = ({ n }) => { const x = n.expansao; return <L itens={[['Promotores (NPS 9-10)', numero(x.promotores)], ['Para Indicações', numero(x.para_indicacao)], ['Renovação', numero(x.renovacao)], ['Upsell', numero(x.upsell)], ['Cross-sell', numero(x.crosssell)], ['Encaminhadas', numero(x.encaminhadas)], ['Convertidas', numero(x.convertidas)], ['Receita de expansão', moeda(x.receita, 0)]]} />; };
