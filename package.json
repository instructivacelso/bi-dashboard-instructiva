{
  "name": "bi-dashboard-instructiva",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "node scripts/migrate.js && next start -p ${PORT:-3000}",
    "migrate": "node scripts/migrate.js",
    "seed": "node scripts/seed.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jose": "^5.9.6",
    "next": "^16.3.5",
    "pg": "^8.13.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  }
}
